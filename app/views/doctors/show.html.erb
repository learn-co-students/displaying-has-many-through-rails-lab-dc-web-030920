<!-- #Create a Doctor#show page that displays the doctor's name, department,  -->
<!-- #and appointments, with each appointment's date, time, and patient name  -->
<!-- #(linking to the patient's show page). -->
<h1>Doctor's Detail</h1>
<h2> <%= @doctor.name %> works at <%= @doctor.department%></h2>
<h3>Appointments</h3>


<ul>
    <% @doctor.appointments.each do |app| %>
       <li> <%= " #{app.appointment_datetime.strftime("%B %d, %Y")} at  #{app.appointment_datetime.strftime("%H:%M")}" %>  </li>
       <li><%= link_to app.patient.name, patient_path(app.patient) %></li>    
    
    page
      
    <% end %>

</ul>



