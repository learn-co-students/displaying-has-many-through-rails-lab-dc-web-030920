<%= @patient.name %>
<% @patient.appointments.each do |appointment| %>
    <% @doctor = @patient.doctors.find(appointment.doctor) %>
    <%= link_to @doctor.name, doctor_path(@doctor) %> - <%= link_to appointment.appointment_datetime.strftime("%B %d, %Y at %k:%M"), appointment_path(appointment) %>
<% end %>