<h1>Doctor: <%= link_to @appointment.doctor.name, doctor_path(@appointment.doctor_id) %>
<h1>Patient: <%= link_to @appointment.patient.name, patient_path(@appointment.patient_id) %>
<h1>Date & Time: <%= @appointment.appointment_datetime.strftime("%B %d, %Y at %k:%M") %>
